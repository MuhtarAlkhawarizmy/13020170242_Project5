<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.malaqbiapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view2.MenuActivity"></activity>
        <activity android:name=".view.about" />
        <activity android:name=".view2.desc_Menu"/>
        <activity android:name=".view2.edit_akun"/>
        <activity android:name=".view.MainActivity" />
        <activity android:name=".behind.login" />
        <activity android:name=".behind.daftar" />
        <activity android:name=".behind.flipper" />
        <activity android:name=".behind.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>